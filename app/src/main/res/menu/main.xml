<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_search"
          android:title="Search"
          app:showAsAction="always" android:icon="@android:drawable/ic_menu_search"/>

    <item android:id="@+id/exampleAddComic"
          android:title="E.g. Add Comic"
          app:showAsAction="never"/>

    <item android:id="@+id/exampleAddFolder"
          android:title="E.g. Add Folder"
          app:showAsAction="never"/>

    <!-- UNUSED /
<item android:id="@+id/nav_home"
      android:title="Home"
      android:icon="@drawable/ic_home"
      app:showAsAction="never"/>

<item android:id="@+id/nav_library"
      android:title="Library"
      android:icon="@drawable/ic_library"
      app:showAsAction="never"/>

<item android:id="@+id/nav_wishlist"
      android:title="Wishlist"
      android:icon="@drawable/ic_wishlist"
      app:showAsAction="never"/>

<item android:id="@+id/nav_store_locator"
      android:title="Store Locator"
      android:icon="@drawable/ic_store"
      app:showAsAction="never"/>

<item android:id="@+id/nav_scan"
      android:title="Scan Comic Covers"
      android:icon="@drawable/ic_scan"
      app:showAsAction="never"/>

<item android:id="@+id/nav_logout"
      android:title="Logout"
      android:icon="@drawable/ic_logout"
      app:showAsAction="never"/>

<item android:id="@+id/action_settings"
      android:title="@string/action_settings"
      android:orderInCategory="100"
      app:showAsAction="never"/>
-->
</menu>

