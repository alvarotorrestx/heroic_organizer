<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context=".MainActivity">

    <include
            android:id="@+id/app_bar_main"
            layout="@layout/app_bar_main" />

    <!-- nav drawer -->
    <LinearLayout
            android:id="@+id/custom_drawer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:fitsSystemWindows="true"
            android:orientation="vertical"
            android:background="#222"
            android:padding="24dp"
            android:layout_marginEnd="-65dp"
            android:layout_marginRight="-65dp">

        <include layout="@layout/nav_header_main"/>

        <!-- divider -->
        <View android:layout_width="match_parent" android:layout_height="1dp" android:background="#444"/>

        <!-- menu items -->
        <LinearLayout
                android:id="@+id/menu_container"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_vertical">

            <TextView android:id="@+id/menu_home" style="@style/MenuItemStyle" android:text="Home"/>
            <TextView android:id="@+id/menu_library" style="@style/MenuItemStyle" android:text="Library"/>
            <TextView android:id="@+id/menu_wishlist" style="@style/MenuItemStyle" android:text="Wish-list"/>
            <TextView android:id="@+id/menu_scan" style="@style/MenuItemStyle" android:text="Scan Comic Cover"/>
            <TextView android:id="@+id/menu_locator" style="@style/MenuItemStyle" android:text="Store Locator"/>
            <TextView android:id="@+id/menu_settings" style="@style/MenuItemStyle" android:text="Settings"/>

        </LinearLayout>
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
